%h4 My Lists:
- if @current_user.lists.count > 0
  %ul
    - @current_user.lists.each do |list|
      %li= link_to list.name, list_path(list)
- else
  None.

=render partial: "shared/beta_list", locals:{betas: @available_betas, title: 'My available' }
=render partial: "shared/beta_list", locals:{betas: @active_betas, title: 'My active' }
=render partial: "shared/beta_list", locals:{betas: @finished_betas, title: 'My finished' }
