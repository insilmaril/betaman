%h2 Update downloads

- @betas.each do |beta|
  %h4= beta.name

  %ul
    -@users_added[beta.id].each do |u|
      %li= "Added: #{u.id} - #{u.email}"

%br
%br
%p
  =link_to "Back to admin dashboard", admin_path, class: 'btn'
